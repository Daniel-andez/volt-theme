{% comment %}
  head-variables.liquid
  ─────────────────────────────────────────────────────────────────────────────
  Translates all theme editor settings into CSS custom properties.
  Included once in <head> via layout/theme.liquid.
  All design tokens live here — components reference var(--volt-*) only.
  ─────────────────────────────────────────────────────────────────────────────
{% endcomment %}

{% liquid
  assign font_heading_scale = settings.font_heading_scale | divided_by: 100.0
  assign body_size_rem      = settings.font_body_size | append: 'px'
%}

<style>
  /* ─── Font Faces (Shopify font API) ─────────────────────────── */
  {{ settings.font_heading | font_face: font_display: 'swap' }}
  {{ settings.font_body    | font_face: font_display: 'swap' }}

  /* ─── Root Design Tokens ────────────────────────────────────── */
  :root {
    /* Colors */
    --volt-accent:              {{ settings.color_accent }};
    --volt-accent-hover:        {{ settings.color_accent_hover }};
    --volt-bg:                  {{ settings.color_background }};
    --volt-surface:             {{ settings.color_surface }};
    --volt-border:              {{ settings.color_border }};

    --volt-text:                {{ settings.color_text_primary }};
    --volt-text-secondary:      {{ settings.color_text_secondary }};
    --volt-text-inverse:        {{ settings.color_text_inverse }};

    --volt-btn-primary-bg:      {{ settings.color_button_primary_bg }};
    --volt-btn-primary-text:    {{ settings.color_button_primary_text }};
    --volt-btn-secondary-bg:    {{ settings.color_button_secondary_bg }};
    --volt-btn-secondary-text:  {{ settings.color_button_secondary_text }};

    --volt-header-bg:           {{ settings.color_header_bg }};
    --volt-header-text:         {{ settings.color_header_text }};
    --volt-footer-bg:           {{ settings.color_footer_bg }};
    --volt-footer-text:         {{ settings.color_footer_text }};

    --volt-success:             {{ settings.color_success }};
    --volt-error:               {{ settings.color_error }};
    --volt-warning:             {{ settings.color_warning }};

    /* Typography */
    --volt-font-heading:        {{ settings.font_heading.family }}, {{ settings.font_heading.fallback_families }};
    --volt-font-body:           {{ settings.font_body.family }},    {{ settings.font_body.fallback_families }};
    --volt-font-heading-weight: {{ settings.font_heading_weight }};
    --volt-font-heading-transform: {{ settings.font_heading_transform }};
    --volt-font-body-size:      {{ body_size_rem }};
    --volt-font-body-lh:        {{ settings.font_body_line_height }};

    /* Heading scale — multiply each heading size by this factor */
    --volt-heading-scale:       {{ font_heading_scale }};

    /* Computed heading sizes */
    --volt-h1: calc(3rem   * var(--volt-heading-scale));
    --volt-h2: calc(2.25rem * var(--volt-heading-scale));
    --volt-h3: calc(1.75rem * var(--volt-heading-scale));
    --volt-h4: calc(1.375rem * var(--volt-heading-scale));
    --volt-h5: calc(1.125rem * var(--volt-heading-scale));
    --volt-h6: calc(1rem    * var(--volt-heading-scale));

    /* Layout */
    --volt-page-width:          {{ settings.layout_page_width }};
    --volt-section-spacing:     {{ settings.layout_section_spacing }}px;
    --volt-grid-gap:            {{ settings.layout_grid_gap }}px;
    --volt-radius:              {{ settings.layout_border_radius }}px;

    /* Header */
    --volt-logo-width:          {{ settings.header_logo_width }}px;

    /* Promo banner */
    --volt-promo-bg:            {{ settings.promo_banner_bg }};
    --volt-promo-text:          {{ settings.promo_banner_text_color }};

    /* Transitions */
    --volt-ease:                cubic-bezier(0.25, 0.46, 0.45, 0.94);
    --volt-duration:            220ms;
    --volt-duration-slow:       400ms;
  }

  /* ─── RTL Support ───────────────────────────────────────────── */
  {% if settings.i18n_rtl_support %}
    html { direction: rtl; }
  {% endif %}

  /* ─── Reduced-motion overrides ──────────────────────────────── */
  {% unless settings.layout_animations_enabled %}
    *, *::before, *::after {
      animation-duration:   0.01ms !important;
      transition-duration:  0.01ms !important;
    }
  {% endunless %}

  /* ─── Global base styles using tokens ──────────────────────── */
  html {
    font-size: var(--volt-font-body-size);
    -webkit-text-size-adjust: 100%;
  }

  body {
    font-family:      var(--volt-font-body);
    line-height:      var(--volt-font-body-lh);
    color:            var(--volt-text);
    background-color: var(--volt-bg);
    margin: 0;
  }

  h1, h2, h3, h4, h5, h6 {
    font-family:    var(--volt-font-heading);
    font-weight:    var(--volt-font-heading-weight);
    text-transform: var(--volt-font-heading-transform);
    line-height:    1.15;
    margin: 0 0 0.5em;
  }

  h1 { font-size: var(--volt-h1); }
  h2 { font-size: var(--volt-h2); }
  h3 { font-size: var(--volt-h3); }
  h4 { font-size: var(--volt-h4); }
  h5 { font-size: var(--volt-h5); }
  h6 { font-size: var(--volt-h6); }
</style>
